import { PrivilegeDirective } from './privilege.directive';

describe('PrivilegeDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new PrivilegeDirective();
    expect(directive).toBeTruthy();
  });
});
